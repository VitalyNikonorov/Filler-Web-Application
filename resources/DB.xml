<class type='DBService.SerializationObject'>
    <hibernate.temp.use_jdbc_metadata_defaults>false</hibernate.temp.use_jdbc_metadata_defaults>

    <hibernate.dialect>org.hibernate.dialect.PostgreSQL9Dialect</hibernate.dialect>

    <hibernate.connection.driver_class>org.postgresql.Driver</hibernate.connection.driver_class>

    <hibernate.connection.url>jdbc:postgresql://localhost:5432/filler</hibernate.connection.url>

    <hibernate.connection.username>postgres</hibernate.connection.username>

    <hibernate.connection.password>test</hibernate.connection.password>

    <hibernate.show_sql>true</hibernate.show_sql>

    <hibernate.hbm2ddl.auto>validate</hibernate.hbm2ddl.auto>

    <hibernate.temp.use_jdbc_metadata_defaults>false</hibernate.temp.use_jdbc_metadata_defaults>
</class>